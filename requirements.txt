# Zola-mac Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
requests>=2.31.0        # HTTP library for API calls
yt-dlp>=2023.0.0        # YouTube video metadata extraction
python-slugify>=8.0.0   # Better slug generation (optional, can use built-in)

# Development dependencies (optional)
pylint>=3.0.0          # Code linting
flake8>=6.1.0          # Style guide enforcement
black>=23.0.0          # Code formatting
pytest>=7.4.0          # Testing framework

# Optional enhancements
python-dotenv>=1.0.0   # Environment variable management
jinja2>=3.1.0          # Advanced templating (if needed)
pyyaml>=6.0            # YAML support for alternative configs
markdown>=3.5.0        # Markdown processing utilities
